exports.commands = function (client, room, chatter, message, self, reportError) {
    if (self) {
        return;
    }

    /*if ((message.toLowerCase()).includes("!currentgame")) {
        client.action(room, 'Working on that command')
    };
    if ((message.toLowerCase()).includes("!title")) {
        client.action(room, 'Working on that command')
    };
    if ((message.toLowerCase()).includes("!uptime")) {
        client.action(room, 'Working on that command')
    };*/
}
