exports.commands = function (room, chatter, message, self, reportError) {
    if (self) {
        return;
    }

    /*if ((message.toLowerCase()).includes("!currentgame")) {
        TwitchClient.action(room, 'Working on that command')
    };
    if ((message.toLowerCase()).includes("!title")) {
        TwitchClient.action(room, 'Working on that command')
    };
    if ((message.toLowerCase()).includes("!uptime")) {
        TwitchClient.action(room, 'Working on that command')
    };*/
}
